{
  "info": {
    "name": "inventory-service",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "List Inventory",
      "request": {
        "method": "GET",
        "header": [
          { "key": "X-Correlation-Id", "value": "{{correlation_id}}" }
        ],
        "url": "{{gateway_url}}/api/v1/inventory"
      }
    },
    {
      "name": "Get Inventory Item",
      "request": {
        "method": "GET",
        "header": [
          { "key": "X-Correlation-Id", "value": "{{correlation_id}}" }
        ],
        "url": "{{gateway_url}}/api/v1/inventory/{{product_id}}"
      }
    },
    {
      "name": "Swagger UI",
      "request": {
        "method": "GET",
        "url": "{{inventory_service_url}}/swagger-ui.html"
      }
    }
  ]
}
